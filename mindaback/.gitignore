node_modules
.env
dist
build
coverage
package-lock.json
.DS_Store
.vscode
.idea
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.env.local
.env.development.local
.env.test.local
.env.production.local
.cache
.next
out
logs
# NOTE: a bare "config" here matched src/config/ (redis.js, mongodb.js,
# MailTransporter.js, sap-materials.sample.json — all essential source, no
# secrets) and silently excluded it from git. Ignore only config.json files.
**/config.json

# Ignore specific script
scripts/migrate-plc-data.js